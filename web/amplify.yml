version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn
    build:
  artifacts:
    baseDirectory: reearth-cms-web
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
