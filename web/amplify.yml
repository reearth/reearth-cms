version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Skipping prebuild phase."
    build:
      commands:
        - echo "Skipping build phase."
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
