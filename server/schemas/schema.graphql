type Schema implements Node {
  id: ID!
  projectId: ID!
  fields: [SchemaField!]!
  titleFieldId: ID
  titleField: SchemaField
  project: Project!
}

# extend type Query {}

# extend type Mutation {}
