package integration

import (
	"context"
)

func (s *Server) ProjectFilter(ctx context.Context, request ProjectFilterRequestObject) (ProjectFilterResponseObject, error) {
	//uc := adapter.Usecases(ctx)
	//op := adapter.Operator(ctx)
	//
	//p, pi, err := uc.Project.FindByWorkspace(ctx, nil, op)
	//if err != nil {
	//	return nil, err
	//}
	//
	//return ProjectFilter200JSONResponse(integrationapi.NewProject(p)), err
	panic("implement me")
}
